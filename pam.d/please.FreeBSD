#
# PAM configuration for the "please" service
#

# auth
auth		sufficient	pam_rootok.so		no_warn
auth		requisite	pam_group.so		no_warn group=wheel fail_safe ruser
auth		include		system

# account
account		include		system
